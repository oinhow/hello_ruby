<%= image_tag @pin.image.url(:medium) %>
<p id="信息"><%= notice %></p>

<p>
  <strong>描述:</strong>
  <%= @pin.description %>
</p>

<% if @pin.user == current_user %>
  <%= link_to '编辑', edit_pin_path(@pin) %>
<% end %>
<%= link_to '返回', pins_path %>
